# Milzet recovery and device repair — 9 September 2026

The original approved development APK was installed as `com.binteca.interactive.milzet.viewer` on Quest 3S `3487C10GBM006L`. Its exact hash and before/after package metadata are in `Artifacts/DeviceAcceptance/install-20260909.json`. Both existing VR Studio package versions, installation paths and update timestamps remained unchanged. The original APK is retained as `Artifacts/DeviceAcceptance/milzet-v1-approved.apk`.

## Failed wearer acceptance

The wearer reported that each of the five procedural packages displayed, but movement/interaction stopped and the system menu restored controller responsiveness. This is a failed acceptance result despite successful rendering and light-scene telemetry. Readability, physical audio, comfort and reliable interaction must be retested.

The complete 33.486-second recording `wearer-121422.mp4` was reviewed through sampled frames. It shows the flat test schematic, demonstration controls, pause cue and overlay, and navigation between Media and Menu. The longer recording was still growing during its initial copy; the retained partial copy has a decode error at its tail. Some frames were reviewed before the user instructed us not to review or copy that recording further. Its partial tail is not a blocker. No recording was deleted.

## Repairs

- Keep the XR origin, tracked camera and controller actions alive across package/experience loads. Previously they were destroyed while the selecting controller action was executing.
- Read `isTracked` through a Value action. Unity Input System Value actions perform an initial state check; the previous Button action did not, so a newly created action could remain false until a device state transition. This mechanism is consistent with recovery after a system-menu cycle; wearer retesting is still required.
- Add left-stick movement, right-stick 30-degree turning, B-button menu recall and a return-to-start control.
- Use the original Viewer's dark slate, teal buttons and sidebar visual organization, with package, inspection, task, playback and movement sections. Original source was read only, with no sibling code/media copied.
- Render the welcome scene before asynchronously loading the first GLB, retaining visible playback while the importer works.
- Replace the nested Base64 regex that overflowed on the actual multi-megabyte generated GLB. A 6 MB regression covers large valid content and malformed padding.

The local `Samples/firsthand` directory contains its README but not the referenced `Assets/Project` implementation; the Discover Assets directory is also absent. No sample repair/download was undertaken. Input behavior was verified against the installed Unity Input System source and current Meta OpenXR documentation.

## Real generation

One Meshy-6 preview and one 2K refinement succeeded for the prepared generic cone. Provider receipts record 20 + 10 credits. One George multilingual-v2 narration succeeded; the history receipt records 118 credits for the 195-character request. No additional generations or purchases occurred.

The MCP audio result used an `.mp3` extension for requested raw PCM; its bytes were wrapped as 24 kHz mono WAV without regeneration. Duration is 12.8174 seconds. Blender inspection found one mesh, 27,477 vertices, 29,823 polygons and four 2048-square textures. Visual review shows an orange cone, white reflective band and black square base.

The actual 7,304,416-byte GLB and WAV are embedded in a test package with an explicitly procedural panorama backdrop. Browser import/render/audio advancement/export/reopen and phone layout passed. The rebuilt native Mac app rendered the cone, advanced the generated audio and completed the scenario using the same package revision. See `Artifacts/Generation/browser-verification.json` and `native-generated-verification.json`. Physical headset audio and generated-content acceptance are separate.

## Validation so far

42 Node tests pass. The updated eight-stage Unity run passes, including persistent rig/camera/actions across all five procedural packages and movement/turn checks using simulated input. Production and WebXR builds pass with the existing bundle-size advisory. Actual Mac generated-content validation passes. These results do not establish a repaired headset pass.

Five client-owned onsite packs and receiving-team interoperability remain external acceptance requirements. CareerWIL retains identity, authoritative progression, grading, evidence, signoffs and stipend responsibilities.

## Later recording and build

The user subsequently requested the completed4:23 recording. The matching `_0_1.mp4` remained108,869,524bytes across30seconds; its actual duration is262.911729seconds. It was pulled separately and fully video-decoded without errors. Sampled frames across the complete timeline show menu/package switches, small truncated names, intentional progression prerequisites, and system-menu return. See `recording-review.json`. This review does not establish physical trigger pressure or prove every reported interaction defect fixed.

Corrected Mac and Android builds completed. APK1.0.1/code2 is46,925,226bytes, SHA256 `bccb845a9e62f41fba041c28ba56a7c81ebce551e5c969f28835767fda868e33`, signed by the same verified debug certificate. ARM64/OpenXR and Quest boot settings pass. Final content audit passes. The corrected APK was installed with data retained at headset local time 12:33:34. Version 1.0.1/code 2 and six package files were confirmed; the app runs as PID 21837. The current system screen requests the power button to enable cameras and microphones. Wearer retesting remains pending and the next action has been sent to the voice coordinator.

Before this corrected update, the legacy `com.binteca.anthvrstudio` package was unexpectedly absent. This task issued no uninstall or clear commands; the cause is unverified. The modern `com.binteca.interactive.vrstudio.viewer` remained version 2.3.0/code 58. Initial preservation evidence applies only to the initial install, not to this later external state.

## Corrected build wearer retest

The wearer reports the menu initially appears at the bottom and cannot be moved; opening presentation 01 relocates it over content. Menu placement and movement acceptance FAILED on 1.0.1. A live screenshot confirms the schematic is loaded, the Packages tab is open and both controller rays are visible. Guidance sent through the coordinator: select Inspect in the left sidebar once to use the loaded package. The app was not restarted or switched. The wearer is recording; further interaction acceptance remains unresolved.

## Grab repair under local validation

The menu now has a dedicated top grab bar. Controller grip/trigger and OpenXR hand pinch/grasp initiate movement; release, tracking loss and focus loss end the grab. The menu canvas survives package changes and retains its pose; automatic initial placement waits for a tracked head pose and places it to the side. Explicit B/Menu here recall remains available. Android hand interaction uses the installed OpenXR HandInteractionProfile and optional handtracking manifest feature.

Eight Unity stages passed after simulated grip/pinch move and release checks and retained pose checks across five packages and tab changes. This is not physical hand/controller acceptance. A 1.0.2/code 3 build is in progress. Expanded content-flow, display-size and all-panel changes are proposed to the coordinator pending confirmation.

The newer stopped recording `com.binteca.interactive.milzet.viewer-20260909-144456-0.mp4` was stable at 76,534,331 bytes across two reads. USB disconnected during the pull, leaving no completed local copy. Its review is pending reconnection.

## Expanded usability and Commons examples

The wearer explicitly confirmed the broader content-flow, all-panel movement, adjustable screen and rounded visual treatment. Menu/reference/display grab bars now share tracked controller/hand movement, released poses persist across package changes, and initial menu/reference placement is to the sides. Flat displays start at a2.4m bounding width, preserve media aspect ratio, and support1.2–4m sizing while hotspot anchors remain attached. Content opens into View/Play with activities when present. Sources shows package attribution. Eight Unity stages pass including independent reference/display drag and reopen persistence; physical acceptance is not established.

The stopped184.935-second recording was pulled and fully decoded after transient device visibility recovered without reboot or clearing data. See `recording-144456-review.json`. No evidence showed physical cable unplugging.

Five online-media demonstration packages were assembled from four licensed/public-domain sources. All five and14 activities pass actual browser and built-Mac playback/completion checks. The NIOSH video is a full-length delivery transcode, not new generated media. These examples do not satisfy the missing onsite captures or client content signoff. See `REQUIREMENTS_REVIEW_20260909.md` for the original-spec matrix and `Artifacts/Commons/README.md` for attribution.

## 1.0.2 headset deployment and runtime findings

After the coordinator relayed the wearer’s explicit release of the headset, 1.0.2/code 3 was installed with replace/keep-data semantics. APK SHA-256: `9cdd61569286009ae8bd04a64476f2a3c4ad44c1d2dfd4ac631786c4f49d232a`, 64,859,429 bytes. All six prior package files remained at their original sizes; five Commons examples were added. The separate VR Studio app was already 2.4.0/code 60 before this operation and retained its installation path and update timestamp afterward.

Milzet launched as PID 8671, reported XR ready, and rendered the welcome screen with controller ray. Android logged missing SphereCollider support: the linker stripped a component created indirectly by CreatePrimitive. This affects hotspot ray selection. Explicit linker preservation is being validated in 1.0.3. The wearer also reported the ray passing targets and did not confirm successful menu movement. Code inspection confirmed a fixed four-metre visual ray. The repair clips its endpoint to the nearest panel/display/physics hit and adds panel identity/position to grab/release diagnostics. Several physical grab/release events occurred, but the old logs do not prove which panel moved or whether the wearer found the interaction usable. Physical acceptance remains failed/pending repair, not passed.

The wearer clarified the failed ray target was the menu top bar and requested whole-panel grabbing. Local correction removes every grab bar, uses controller grip over the entire surface, and distinguishes stationary hand pinch activation from pinch-and-move drag (2.5 cm / 4 degree threshold). Releasing a drag does not activate a button. All eight Unity stages pass after this refinement, including the hand tap/drag distinction. A reported overhead moving bar/ball remains unidentified because it was outside the captured frame; current logs show the full-sphere blue video fixture, which has no presentation guide path.

A later screenshot directly captured the cyan guide line/cursor above the trench schematic. This identified the reported overhead object. For flat lessons the guide is now attached to the display with its source x/y coordinates fitted to the plate and a small front offset; it scales and moves with the lesson. Immersive/world presentations retain their spatial guide coordinates. Menu body text now wraps into TMP pages with Read more, at unchanged font size, and the physical panel width increases to 0.99 m. The visible navigation starts with Lessons, directly listing packages; startup opens that list and content opens View/Play. These later changes supersede the intermediate whole-panel-only build.

## Approved full startup restoration

The user explicitly approved original animated boot/setup, saved hand and posture choices, actual scene permission/system Space Setup/MRUK loading and retry, eye-relative panels and valid room geometry. Source was inspected read-only in the current original VR Studio project. No edits were made there or in Physio. Original boot/UI dependencies and license were copied with checksums; Meta XR Core and MRUK 85.0.0 use the same installed generation as that source. The client library remains empty.

The final local run passes nine Unity stages. New checks exercise actual animation-to-setup, confirmed-only preferences, mirrored seated/standing placement without changing head position, simulated permission denial/unavailable-room/retry, room geometry clearance and long text pagination. These are not physical room-scan results. The real adapter requests USE_SCENE, invokes system Space Setup and then requires usable MRUK room geometry; it does not equate a successful setup-return value with a fresh successful scan. 1.1.0/code 5 Mac and Android builds are running; the headset remains 1.0.2 pending coordinated replacement and wearer acceptance.

SDK packaging exposed three integration requirements: a macOS microphone usage description for an optional SDK audio assembly (recording remains unimplemented), active Android target selection before SDK manifest generation, and Linear color space for Meta OpenXR. The rebuilt Mac universal app also lacked MRUK’s ARM64-only dylib and failed its new smoke test; the Mac build is now targeting ARM64, with library and runtime checks still pending. All nine Unity stages pass under Linear lighting. A head-driver handoff bug was fixed by retaining enabled head-state input actions while the Meta rig drives camera pose. No new headset install occurred during these repairs.

The corrected Mac build uses the platform Architecture setting (arm64), since PlayerSettings.SetArchitecture alone still produced a universal player. Actual output is Mach-O arm64 with Contents/Plugins/ARM64/libmrutilitykitshared.dylib. The 1.1.0 Mac Commons smoke test now passes five packages, 14 activities and actual video advancement, exit zero, with no MRUK load exception. Receipt: Artifacts/Commons/native-verification.json; log: native-1.1.0-fixed.log. Android Linear build is running separately; no headset action was performed.

Final 1.1.0/code 5 Android build succeeded at 2026-09-09T14:14:05Z. Verified existing signer and v2 signature, hand/scene/anchor permissions, ARM64 OpenXR/OVRPlugin/MRUK libraries, and generated SphereCollider bindings. APK is 66,307,894 bytes, SHA-256 79115f9238e7803f42c2b95b5e3e323167efef0fd1176d4bceeb427ffcfb68cd; archived milzet-v1.1.0.apk. Content audit and git diff check pass. Correctly invoked generated-asset 1.1.0 Mac check also passes cone visibility, audio time advancement and completion. No device commands issued; coordinator reserved current Quest window for main VR Studio61, so Milzet awaits a subsequent window.

Physical 1.1.0 wearer acceptance FAILED: repeated hand aim grabs, reversed/tilted menu and missing initial setup. Releases are logged (including focus loss), so permanent latch/parenting is not proven. Local1.1.1 removes hand grasp-as-grip priority, drags from pinch position without hand aim rotation, rearms only after neutral input following focus/tracking return, and uses OVR head tracking for Android boot gating. User approved Exit App; it pauses media/releases grabs/requests quit without clearing data. Reference card polish adds an inset aspect-preserved image, full Reference header, wrapped/paged body and ray-clickable Read more. The fixture main/reference PNG hashes are identical; retain both and label same source image. Recording162607 is archived and sampled across58.215s; duplicate-timestamp muxer warnings exist, no decoder error observed. Controllers later report active/position tracked and Touch clicks are logged, but no outside-app baseline or proof of Bluetooth unpairing. Current selected Presentation test is a three-second PNG/narration guide with authored pause at1s, not a video. These source repairs are not deployed.

User approved1.1.1 replacement window. Subsequent live metavr discovery/info report3487C10GBM006L unauthorized, transport1; no update or launch occurred. User reports no debugging dialog visible. No controller re-pair, key revocation, developer-setting reset or ADB server reset was issued. Installation approval persists; Mac1.1.1 consumer verification continues independently while USB access is blocked.

USB authorization returned on rediscovery. Executed approved1.1.1/code6 replace-install17:35:28; installed APK exact SHA256 match,11package listings and other app metadata unchanged. Launched PID25090. Fresh screenshot shows physical-power sensor lock; coordinator informed. Mac1.1.1 actual five-example/14activity/video and generated cone/narration consumer checks pass. Physical startup acceptance is pending wearer.

Resume check: Quest authorized, PID25090 alive, current screenshot still shows physical-power sensor lock. Guardian system dialog owns focus, Milzet lacks focus, both controllers connected-inactive/tracking NONE. No restart/reinstall performed. Coordinator has one next wearer action: brief physical headset power press.1.1.1 acceptance remains pending, distinct from1.1.0 failure. Requirements matrix refreshed with this distinction and original startup/room validation gate; onsite client media and CareerWIL integration gaps remain explicit.
